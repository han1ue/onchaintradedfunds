import { SectionCard } from "@/components/ui";
export const metadata = { title: "Privacy" };
export default function PrivacyPage() { return <div className="pageShell contentPage"><header className="pageHeader"><h1>Privacy</h1><p>How the launch competition uses X identity and verification data.</p></header><SectionCard className="contentCard"><h2>Data we keep</h2><p>We store your immutable X user ID, current handle and public profile metrics observed when you act, together with proposal, vote, post, moderation and validity history. Your X OAuth access and refresh tokens are encrypted before database storage and used only for posts you explicitly approve.</p><h2>X posts</h2><p>We retain post IDs, URLs, hashes, timestamps, edit history identifiers and verification outcomes. Raw post text is scheduled for deletion after 30 days unless an action is disputed.</p><h2>Network data</h2><p>Rate limits use a rotating HMAC of the requesting IP address. Application tables do not store raw IP addresses.</p><h2>Deletion</h2><p>Contact the project administrator to request deletion of optional profile data. Competition evidence may be retained where necessary to preserve the integrity and auditability of published results.</p></SectionCard></div>; }
