import { Callout, SectionCard } from "@/components/ui";
import Link from "next/link";
import { getCompetition } from "@/server/data";

export const metadata = { title: "Competition rules" };
export default async function RulesPage() { const competition = await getCompetition(); const rules = competition.rules; return <div className="pageShell contentPage"><header className="pageHeader"><h1>Competition rules</h1><p>A simple, auditable path from community proposal to main-app launch order.</p></header><SectionCard className="contentCard">
  <Callout><strong>Frozen rules snapshot.</strong> <span className="rulesSnapshot">SHA-256 <code>{competition.rulesHash}</code> · Frozen {new Date(competition.rulesFrozenAt).toLocaleString("en-US", { month: "short", day: "numeric", year: "numeric", hour: "numeric", minute: "2-digit", timeZone: "UTC", timeZoneName: "short" })}.</span></Callout>
  <h2>Who can participate</h2><p>Anyone can browse. Creating an OTF or voting requires a verified, public X account with at least {rules.minFollowers.toLocaleString()} followers that is at least {rules.minAccountAgeDays} days old. Your handle is refreshed whenever you connect with X; reconnecting does not refresh the other stored eligibility details. A fixed operator allowlist may waive verification and follower-count requirements; public-account and account-age requirements still apply. Follower-count or verification changes after a proposal or vote is accepted do not retroactively invalidate that accepted action.</p>
  <h2>Creating an OTF</h2><ul><li>The first {rules.submissionOnlyDays} days are reserved for submissions. Voting opens on competition day {rules.submissionOnlyDays + 1}, and submissions remain open throughout the {rules.votingDays}-day voting phase.</li><li>{rules.maxProposalsPerAccount === null ? "Each eligible X account may confirm as many OTF proposals as it wants in this competition." : <>Each eligible X account may have up to {rules.maxProposalsPerAccount} confirmed OTF proposals in this competition. Drafts, expired drafts, and deleted proposals do not count; deleting a confirmed proposal frees a slot.</>}</li><li>Saved drafts expire after 24 hours. Expired drafts cannot be resumed, and their name, ticker, and URL slug become available again.</li><li>A confirmed submission may be deleted while submissions are open. Votes already cast for a deleted OTF remain spent and cannot be reassigned, but they are excluded from launch ranking and Participation, Performance, and Creator XP.</li><li>The name must end in “OTF”; tickers are uppercase and no longer than 16 characters.</li><li>Choose at least {rules.minAssets} distinct 18-decimal assets and allocate at least {rules.minAssetWeightBps / 100}% to each. <Link className="inlineLink" href="/assets">View the asset registry</Link>.</li><li>Verified assets use the price source stored in their registry row: Robinhood bids for stock tokens and Coinbase ETH/USD bids for ETH exposure. Submitters cannot override these sources.</li><li>Unlisted assets are checked server-side through Robinhood RPC first. Their pool must be a canonical Uniswap V3 pool containing the asset and exactly canonical WETH or USDG, with a supported fee, initialization, at least 64 observations, and a successful one-hour observation. GeckoTerminal evidence must then show at least $30,000 liquidity, at least $100,000 verified market cap (never FDV), seven-day pool age, GT verification, GT score at least 50, no honeypot flag, and at least 50% locked liquidity.</li><li>Weights must total exactly {rules.portfolioWeightBps / 100}%.</li><li>Add your own optional context, publish the prepared post from X, and paste its public URL for verification.</li><li>An OTF is Verified only while every current constituent is verified; otherwise it is Non-verified. Verification selects the performance XP pool but never affects eligibility, pricing, fees, permissions, votes, or launch order.</li><li>Competition return snapshots come from the external API selected by each asset&apos;s saved price source. Missing competition prices make performance Pending but do not affect votes or launch rank.</li></ul>
  <h2 id="voting">Voting</h2><ul><li>Voting begins after the {rules.submissionOnlyDays}-day submission-only phase. Each eligible X account starts with {rules.initialVotes} unlocked votes.</li><li>{rules.votesPerUnlock} additional vote unlocks every {rules.voteUnlockIntervalDays} voting days, up to {rules.totalVotes} votes total. No vote is added on the final voting day.</li><li>Unlocked votes may be cast across one or more OTF proposals. Creators may vote for any proposal, including their own.</li><li>Once cast, a vote cannot be moved, removed, redistributed, or made available again.</li><li>Every voting action requires a public X post. Several unlocked votes may be cast together and verified by one post.</li><li>OTF picks are omitted from each post by default and appear only when the voter chooses to reveal the OTFs and vote counts in that batch.</li><li>Every required voting post must remain public and unchanged, including after the competition ends. Deleting or editing a voting post is prohibited and never restores its spent vote slots.</li></ul>
  <h2 id="ranking-and-launch-order">Ranking and launch order</h2><p>Votes from valid activated ballots count. Proposals sort by allocated votes descending, then earlier submission time, then immutable proposal ID. Ranks are unique ordinal positions.</p><Callout>The final ranking directly becomes main-app launch order: #1 first, #2 second, #3 third, and so on. Public pages show order only—not internal dates or readiness windows.</Callout>
  <h2>Moderation</h2><p>Manipulation, impersonation, automated abuse, deleted evidence posts, or invalid asset records may invalidate an action. Later fraud voids the affected slot; ranks are not renumbered.</p>
  <h2 id="xp-allocation">XP allocation</h2><p>Exactly 10,000,000 XP will be allocated once, after the competition closes and the final audit is complete: 3,500,000 verified-performance XP, 1,750,000 non-verified-performance XP, 2,750,000 participation XP, and 2,000,000 creator XP. No provisional XP balance or ranking is calculated or published during the competition.</p>
  <h2>Performance XP</h2><p>Every verified vote batch creates a separate immutable tranche. Votes for Verified OTFs compete for a performance XP pool that is 100% larger: 3,500,000 XP versus 1,750,000 XP for Non-verified OTFs. Individual awards depend on relative score within the applicable pool. Vote activation references the newest complete saved price checkpoint at or before activation; it never calls a provider. Market cap, FDV, and token supply never enter return calculations.</p>
  <h2>Participation and creator XP</h2><p>Participation uses valid vote units only and never follower count. The full 2,000,000 creator pool is divided among proposal creators in direct proportion to the valid votes their proposals receive.</p>
  <h2>Final allocation</h2><p>If either performance pool has no eligible score, that pool rolls to participation. After close, the OTF team exports the frozen competition inputs, calculates the final XP allocation outside the launch app, verifies the 10,000,000 XP total, and publishes the result. <Link className="inlineLink" href="/xp">View the allocation method</Link>.</p><Callout tone="warning">XP has no guaranteed monetary value. It is separate from launch ranking, which continues to use valid votes and the published tie-breakers only.</Callout>
  <h2>Changes</h2><p>The hashed rules snapshot cannot change after the competition opens. Non-material clarifications may be published here with their effective time and recorded in the audit history; a material rule change requires a new competition snapshot.</p>
</SectionCard></div>; }
