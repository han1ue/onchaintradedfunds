import { redirect } from "next/navigation";
import { Database, FileCheck2, RefreshCw, Shield } from "lucide-react";
import { SectionCard } from "@/components/ui";
import { auth } from "@/server/auth";
import { adminXIds } from "@/server/env";
export const metadata = { title: "Admin" };
export default async function AdminPage() { const session = await auth(); if (!session?.user?.xUserId || !adminXIds.has(session.user.xUserId)) redirect("/"); return <div className="pageShell contentPage"><header className="pageHeader"><h1>Competition operations</h1><p>Launch-only controls. Every mutation requires an allowlisted X ID and a written audit reason.</p></header><div className="adminGrid"><SectionCard className="compactInfo"><Database /><div><strong>Asset metadata</strong><p>The database maintains names, token contracts, quality labels, and convenient pricing suggestions. Every proposal snapshots its exact user-selected pricing addresses.</p></div></SectionCard><SectionCard className="compactInfo"><Shield /><div><strong>Moderation</strong><p>Hide or disqualify proposals, invalidate evidence, and preserve before/after state.</p></div></SectionCard><SectionCard className="compactInfo"><RefreshCw /><div><strong>Evidence checks</strong><p>Retry unavailable X reads and inspect post validity history.</p></div></SectionCard><SectionCard className="compactInfo"><FileCheck2 /><div><strong>Finalization</strong><p>Run resumable audits, publish one immutable ranking transaction, and export checksummed order files.</p></div></SectionCard></div></div>; }
