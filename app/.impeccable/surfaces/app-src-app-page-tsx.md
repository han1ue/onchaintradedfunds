---
version: 1
slug: "app-src-app-page-tsx"
primary_target: "src/app/page.tsx"
related_targets: ["src/components/OperateExperience.tsx", "src/app/globals.css"]
---

# Swap operate surface

- **Mode:** Operate. A wallet user chooses a pair, understands whether an executable quote exists, and can inspect the limited routes actually queried.
- **Audience and task:** Investors moving between ERC-20 assets and OTF share tokens; creators reach the separate formation form. Fund routes retain the factory-backed directory and address routing while detail pages are stripped to identity/history truth.
- **Constraints:** Retain the dark OTF field, teal accent, compact finance typography, fixed Testnet warning, and explicit experimental status. Do not present verification as pool, route, liquidity, economic, or investment safety. Do not synthesize quotes, submissions, balances, fund rows, or successful transactions.
- **Direction:** A centered, instrument-like swap card with two value fields, a single reversible seam, and a factual route ledger beneath. Supporting pages use the same ruled surface rather than a dashboard-card grid.
- **Memorable moment:** The middle reversal control is the fulcrum between pay and receive; route selection is visibly unavailable until it is genuinely quoted.
- **Unresolved:** Trusted endpoint and deployment configuration plus an authenticated creation-snapshot service are needed before execution can be enabled. The authority signature must bind the chain, factory, intended creator, ordered constituents and token decimals, market caps and unit prices, snapshot time and expiry, calculation version, and nonce; metadata, beneficiary, and annual ratio remain creator choices.
